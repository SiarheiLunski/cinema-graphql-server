schema: schema.graphql
generates:
  ./src/types/schema.d.ts:
    schema: 
      - 'src/modules/**/*.graphql'
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
